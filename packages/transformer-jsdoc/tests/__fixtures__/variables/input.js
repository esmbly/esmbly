/**
 * @type {number}
 */
var a = 1; // eslint-disable-line

/**
 * @type {number}
 */
let b = 1; // eslint-disable-line

/**
 * @type {number}
 * @const
 */
export const c = 2; // eslint-disable-line

/**
 * @returns {number}
 */
function d() { // eslint-disable-line
  /**
   * @type {number}
   */
  const e = 3; // eslint-disable-line
  return e;
}
